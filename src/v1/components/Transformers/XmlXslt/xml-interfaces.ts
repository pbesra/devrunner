export interface XmlInstantReducerProps {
	XML: boolean;
	XSL: boolean;
	CALCULATE_XSLT: boolean;
}
export interface MinimiseState {
	xml: {
		isMinimise: boolean;
	};
	xsl: {
		isMinimise: boolean;
	};
}
