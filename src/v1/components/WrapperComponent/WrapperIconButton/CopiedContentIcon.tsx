import DoneIcon from "@mui/icons-material/Done";
import IconButton from "@mui/material/IconButton";

const CopiedContentIcon = () => {
	return (
		<IconButton>
			<DoneIcon sx={{ fontSize: "16px" }} />
		</IconButton>
	);
};

export default CopiedContentIcon;
